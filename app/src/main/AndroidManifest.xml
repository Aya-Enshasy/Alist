<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.alist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alist">
        <activity android:name=".activity.ComplaintDetails"></activity>
        <activity
            android:name=".activity.BottomSheet"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".activity.ComplaintsArchive"
            android:theme="@style/Theme.Alist.NoActionBar" />
        <activity
            android:name=".activity.SubmitComplaints"
            android:theme="@style/Theme.Alist.NoActionBar" />
        <activity
            android:name=".activity.Complaints"
            android:theme="@style/Theme.Alist.NoActionBar" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/Theme.Alist.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>